/**
 * 
 * @param {number} a 引数1
 * @param {number} b 引数2
 * @returns {number}
 */
export function sum(a, b) {
    return a + b;
}

/**
 * 
 * @param {number} a 引数1
 * @param {number} b 引数2
 * @returns {number}
 */
 export function sub(a, b) {
    return a - b;
}

/**
 * 
 * @param {number} a 引数1
 * @param {number} b 引数2
 * @returns {boolean}
 */
export function gt(a, b) {
    if(a > b) {
        return true;
    } else {
        return false;
    }
}
